<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.crawler.mysql.mapper.ProxyEntityMapper" >

  <insert id="batchInsert">
    replace into proxy_entity (host, port, location,
      type, anonymous_type, res_time, 
      is_using, enable, create_time
      )
    values
      <foreach collection="proxyEntityList" item="item" separator=",">
        (#{item.host,jdbcType=VARCHAR}, #{item.port,jdbcType=INTEGER}, #{item.location,jdbcType=VARCHAR},
        #{item.type,jdbcType=VARCHAR}, #{item.anonymousType,jdbcType=VARCHAR}, #{item.resTime,jdbcType=VARCHAR},
        #{item.isUsing,jdbcType=BIT}, #{item.enable,jdbcType=BIT}, #{item.createTime,jdbcType=TIMESTAMP}
        )
      </foreach>
  </insert>

</mapper>